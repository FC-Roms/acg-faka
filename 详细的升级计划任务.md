# acg-faka 上游 3.5.4 详细升级计划任务

## 1. 升级目标与不可变约束

- 当前仓库：`FC-Roms/acg-faka`，当前分支 `main`，升级前提交 `1e04467`。
- 上游仓库：`lizhipay/acg-faka`，远端名 `upstream`。
- 共同基线：`dde35f4`（3.4.9）。
- 目标版本：`f2442de`（3.5.4）。
- 分叉规模：本地 6 个提交，上游 10 个提交。
- 核心约束：上游 3.5.0～3.5.4 的功能、修复和资源全部纳入；本地二开功能、路由、插件、主题、接口、数据库字段及品牌化安装行为不得被覆盖或降级。
- 工作区要求：不回退已有提交，不做无关重构，不运行 test；使用静态检查、语法检查、Git 差异核对和关键调用链核对代替。

## 2. 升级前差异审计结论

### 2.1 本地二开提交与功能清单

| 提交 | 主要二开内容 | 升级后的保留标准 |
| --- | --- | --- |
| `f70c814` | 新客绑定优惠券、第三方卡密接口、ApiNotification/Remember 插件、订单提取状态、安装 SQL 品牌化 | 优惠券绑定与限制规则、卡密通知/补发/提取链路、Remember 插件和管理端提取状态完整可达 |
| `3928def` | 一键发货清理卡密、商品虚拟销量 | 商品 `sold_base`、显示销量计算、发货后卡密清理及前后端字段完整保留 |
| `4b52c44` | 新人优惠券、邀请奖励插件 | 优惠券 `user_limit`、邀请关系与奖励发放、后台配置和用户入口完整保留 |
| `9d9059b` | 邀请奖励优化、MountFuji 模板 | MountFuji 全量资源、主题配置及邀请奖励页面完整保留 |
| `85d4bcf` | 专属优惠券、用户中心优惠券钱包 | Coupon Open API、QQ 专属优惠券恢复、钱包页面与接口、相关路由完整保留 |
| `1e04467` | 邀请奖励重构、路由规范化、插件状态同步 | 新路由结构、菜单可见性、插件启停状态落盘和重构后的邀请服务完整保留 |

本地相对 3.4.9 共改动 111 个文件，主要保护域如下：

1. 优惠券域：`app/Controller/Api/Coupon.php`、`app/Controller/Coupon.php`、`app/Controller/User/Api/CouponWallet.php`、`app/Support/CouponOpenApi.php`、`config/coupon_api.php` 及对应前端页面/脚本。
2. 卡密通知域：`app/Plugin/ApiNotification/**`、`app/Controller/Opencard.php`、订单提取状态接口与管理端展示。
3. 邀请奖励域：`app/Plugin/InviteReward/**`、`app/Controller/User/InviteReward.php`、Cartoon/MountFuji 邀请页面和菜单控制。
4. 商品二开域：虚拟销量 `sold_base`、真实销量合并展示、一键发货清卡密。
5. 主题与插件域：`app/View/User/Theme/MountFuji/**`、`app/Plugin/Remember/**`、插件启停状态同步。
6. 安装数据域：固定 `acg_` 表前缀、`极智铺` 品牌默认值、优惠券 `user_limit` 字段及索引。

### 2.2 上游 3.5.0～3.5.4 升级范围

上游相对 3.4.9 共改动 186 个文件，升级内容主要包括：

1. Docker：`Dockerfile`、`docker-compose.yml`、`docker/**`、`.dockerignore`。
2. 后台与用户端 UI：Material/Glass 样式、组件、Dashboard 重构、响应式页面与脚本更新。
3. 工单与站内消息：Controller、Model、Service、页面、脚本及安装表结构。
4. 安全与管理：Google 2FA、后台入口工具、节流控制和操作日志补充。
5. 文件管理与上传：后台文件接口、页面和脚本。
6. 商品分类：无限级分类路径与后台展示。
7. 用户中心：PC/移动端主题独立配置、推广页面及相关绑定服务更新。
8. 修复与版本：卡密导出文件名数量修复，`config/app.php` 升级到 3.5.4。

### 2.3 文件重叠与冲突预演

- 双边同时修改：17 个文件。
- Git 可自动合并：10 个文件。
- 必须人工解决：7 个文件。

#### 人工冲突文件处理矩阵

| 文件 | 本地必须保留 | 上游必须纳入 | 合并决策 |
| --- | --- | --- | --- |
| `app/Controller/Admin/Api/Commodity.php` | `ensureSoldBaseColumn()`、`order_sold_real`、`getDisplaySold()`、`sold_base` 保存 | 分类 `flatMap()`、`resolvePath()`、`category_path` | 同一数据循环内同时计算 `order_sold` 和 `category_path`；保存前继续规范化 `sold_base` |
| `app/Controller/Admin/Api/Plugin.php` | 仅状态提交时启停、`syncPluginStatus()` 将状态写回配置 | `ManageLog` 引入及启停、保存配置、主题配置、清日志审计 | 保留本地状态判断与落盘，并在成功动作处补齐上游管理日志；不恢复会吞掉其他配置字段的旧早退逻辑 |
| `app/Controller/Base/View/User.php` | `showInviteRewardMenu` 数据注入及插件状态判断 | `user_center_mobile_theme` 的移动端主题选择 | `render()`/`theme()` 均注入邀请菜单状态，同时采用上游 PC/移动端用户中心主题选择逻辑 |
| `app/View/Admin/Dashboard/Index.html` | 隐藏官方公告区域 | 上游 Dashboard 新结构、Material 组件和统计卡片 | 以上游页面为主体，继续移除/隐藏公告块，并让主内容占满宽度，避免留下空白列 |
| `assets/admin/controller/trade/order.js` | ApiNotification 提取状态转义、批量查询、状态列与刷新 | 上游订单表列、筛选、导出和新版组件调用 | 以上游脚本为主体，在新版表格生命周期中重新接入提取状态列及加载函数 |
| `assets/admin/js/_admin.js` | 本地优惠券使用限制字典 | 上游 3.5.4 压缩运行资源 | 以上游压缩资源为主体，补入与 `assets/admin/js/dict.js` 一致的本地字典；核对开发/压缩两份资源标识一致 |
| `kernel/Install/Install.sql` | 固定 `acg_` 前缀、品牌默认值、`coupon.user_limit` 及索引 | 移动端主题配置、`manage.google_secret`、工单/消息表及全部外键 | 保持本地 `acg_` 安装语义；将上游新增表、索引、约束统一转换为 `acg_`，杜绝 `acg_` 与 `__PREFIX__` 混用 |

#### 自动合并重叠文件复核矩阵

| 文件 | 重点复核内容 |
| --- | --- |
| `app/Controller/User/Api/Index.php` | 本地新客/卡密相关返回逻辑与上游新增数据均存在 |
| `app/Service/Bind/Order.php` | 本地优惠券/邀请奖励 Hook 与上游订单服务更新调用顺序正确 |
| `app/View/User/Theme/Cartoon/Config.php` | 本地优惠券钱包/邀请页面与上游用户中心新页面路由同时注册 |
| `assets/admin/controller/config/plugin.js` | 本地插件操作与上游 UI 交互兼容 |
| `assets/admin/controller/trade/commodity.js` | `sold_base` 表单/列与上游分类路径列同时存在 |
| `assets/admin/controller/trade/coupon.js` | `user_limit` 与上游 UI 更新同时存在 |
| `assets/admin/js/dict.js` | 本地优惠券字典与上游新增字典同时存在 |
| `assets/user/controller/business/commodity.js` | 商户端虚拟销量与上游商品改动同时存在 |
| `assets/user/controller/business/coupon.js` | 商户端 `user_limit` 与上游改动同时存在 |
| `kernel/Kernel.php` | 本地 Coupon/OpenCard/InviteReward 路由与上游 Message/Ticket/Promote 等路由均可达且无重复覆盖 |

## 3. 执行任务清单

### 阶段 A：建立可恢复点

- [x] A1. 再次确认 `main` 无未提交的用户改动。
- [x] A2. 在升级前 HEAD `1e04467` 创建备份分支 `backup/pre-upstream-3.5.4-20260722`。
- [x] A3. 记录 `origin/main`、`upstream/main` 和 merge-base，确保可审计与回退。

验收：备份分支准确指向 `1e04467`，主分支仍未改变。

### 阶段 B：执行三方合并

- [x] B1. 在 `main` 执行 `git merge --no-ff --no-commit upstream/main`。
- [x] B2. 确认实际冲突集与预演一致；若出现新增冲突，先补充本计划再处理。
- [x] B3. 非重叠上游文件全部保留；本地独有文件不得删除。

验收：冲突仅在已识别范围内，工作区进入可控的未提交合并状态。

### 阶段 C：人工解决 7 个冲突

- [x] C1. 合并商品后台接口，同时保留虚拟销量和无限级分类路径。
- [x] C2. 合并插件后台接口，同时保留状态同步和上游管理日志。
- [x] C3. 合并用户视图基类，同时保留邀请菜单控制和移动端用户中心主题。
- [x] C4. 采用上游 Dashboard，新版布局继续隐藏官方公告。
- [x] C5. 在上游订单脚本中恢复卡密提取状态列、批量状态查询和刷新触发。
- [x] C6. 采用上游 `_admin.js` 并恢复本地优惠券字典，保证与源文件一致。
- [x] C7. 整合安装 SQL：统一 `acg_` 前缀，保留本地品牌/字段，并加入上游新表结构。

验收：`git diff --check` 无冲突标记/空白错误；`git ls-files -u` 为空。

### 阶段 D：复核 10 个自动合并重叠文件

- [x] D1. 逐一阅读自动合并结果，不因“无冲突”而默认正确。
- [x] D2. 核对路由表同时包含本地 Coupon/OpenCard/InviteReward 与上游 Message/Ticket/Promote/File 等入口。
- [x] D3. 核对订单绑定服务中的优惠券、邀请奖励、消息/工单相关逻辑没有互相短路。
- [x] D4. 核对商品/优惠券前后端字段名一致，压缩与非压缩字典一致。

验收：17 个重叠文件的双方语义均有明确落点。

### 阶段 E：本地二开完整性专项核对

- [x] E1. 用升级前提交到合并结果的路径级清单核对 111 个本地改动文件，确认无意外删除。
- [x] E2. 核对 `ApiNotification`、`InviteReward`、`Remember` 三个插件目录与配置入口完整。
- [x] E3. 核对 Coupon Open API、QQ 专属优惠券、优惠券钱包与新客限制的 Controller/Support/Config/View/JS 链路。
- [x] E4. 核对 MountFuji 主题全量文件及新增用户中心菜单配置。
- [x] E5. 核对虚拟销量、发货清卡密、订单提取状态的后端字段和前端展示。
- [x] E6. 核对安装 SQL 中不存在 `__PREFIX__` 残留，所有新表外键均指向 `acg_` 表。

验收：本地独有文件全数存在；关键符号、路由和 Hook 均可静态追踪到调用点。

### 阶段 F：非 test 验证与提交

- [x] F1. 对所有变更 PHP 文件执行 `php -l` 语法检查（若本机 PHP 可用）。
- [x] F2. 对关键 JavaScript 文件执行语法检查（若本机 Node.js 可用），不执行任何 test runner。
- [x] F3. 执行 `git diff --check`、冲突标记扫描、版本号核对、SQL 前缀/外键一致性检查。
- [ ] F4. 对比 `HEAD^1`（本地升级前）与合并结果，形成二开保留清单；对比 `HEAD^2`（上游）确认升级内容已纳入。
- [ ] F5. 更新本文档勾选状态和“执行结果”，创建明确的升级合并提交。

验收：所有可执行的非 test 检查通过；未运行 test 的边界在交付说明中明确记录。

## 4. 风险与回退策略

1. **安装 SQL 前缀风险**：本地已将上游占位符改为固定 `acg_`。本次按“保留本地二开”处理，新增上游表也统一为 `acg_`。这只影响全新安装脚本，不会直接迁移现有数据库。
2. **现有数据库升级风险**：`Install.sql` 不是在线迁移脚本；部署前仍需确认生产库已具备 3.5.x 新字段/新表，尤其 `google_secret`、工单、消息、移动端主题配置及本地 `user_limit`/`sold_base`。
3. **压缩资源风险**：项目未提供明确前端构建脚本，`_admin.js` 需人工保持与源字典一致，因此必须做关键标识双向核对。
4. **主题兼容风险**：上游主要更新 Cartoon，MountFuji 是本地独有主题；通过基类数据注入和路由兼容保证功能，但仍建议部署后对两个主题进行人工页面验收。
5. **回退方式**：任何不可接受结果均可从 `backup/pre-upstream-3.5.4-20260722` 恢复升级前代码；不使用破坏性 reset 覆盖用户工作。

## 5. 部署后建议人工验收场景（不属于本次 test）

1. 管理后台：登录/Google 2FA、Dashboard、商品编辑（虚拟销量+分类路径）、优惠券限制、插件启停、订单提取状态、工单、站内消息、文件管理。
2. 用户中心：PC/移动主题切换、优惠券钱包、邀请奖励入口、消息、工单、推广页面。
3. 下单链路：新客优惠券、QQ 专属优惠券、邀请奖励触发、一键发货清卡密、第三方卡密通知与提取。
4. 主题：Cartoon 与 MountFuji 分别检查导航、邀请奖励、优惠券钱包及新增上游页面的可达性。
5. 全新安装：仅在隔离环境验证 `acg_` 表结构、工单/消息外键和默认品牌配置，不对生产数据库直接执行 `Install.sql`。

## 6. 执行结果

已按计划完成合并内容与提交前验证：

- 备份分支：`backup/pre-upstream-3.5.4-20260722`，指向升级前提交 `1e04467`。
- 合并父提交：本地 `1e04467`，上游 `f2442de`（3.5.4）；正式 merge commit 将在本文档本次更新后创建。
- 实际冲突：与预演一致，共 7 个，无新增冲突；全部按人工冲突矩阵完成双边语义保留。
- 非重叠文件：94 个本地独有改动文件与升级前完全一致；169 个上游独有改动文件与上游 3.5.4 完全一致。
- JavaScript：57 个变更文件已通过 `node --check`。
- PHP：本机未安装 PHP，57 个变更 PHP 文件未能执行 `php -l`；已完成冲突标记、关键符号和调用链静态核对。
- SQL：26 个建表目标和 8 个外键引用均使用 `acg_`，`__PREFIX__` 残留、非 `acg_` 表名和缺失引用均为 0。
- Git：`git diff --cached --check` 通过，未解决冲突数为 0。
- 未运行 test：遵循本工作区指令，未执行任何 test 命令。
