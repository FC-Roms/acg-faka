# ACG-FAKA 项目说明文档

## 项目概述

**ACG-FAKA** 是一款基于 PHP 8.0 开发的原生虚拟发卡系统、个人店铺系统。

- 官方文档：https://faka.wiki
- 项目地址：https://github.com/lizhipay/acg-faka
- 当前版本：3.4.9

---

## 技术框架与架构

### 技术栈概览

| 组件 | 技术方案 |
|------|----------|
| **编程语言** | PHP 8.0+ |
| **核心框架** | 自定义 MVC 框架（Kernel.php） |
| **数据库 ORM** | Laravel Eloquent ORM (illuminate/database ^7.30.6) |
| **模板引擎** | Smarty 3.1 |
| **数据库** | MySQL 5.6+ |
| **邮件服务** | PHPMailer 6.5 |
| **HTTP 客户端** | GuzzleHTTP 7.4 |
| **认证机制** | Firebase PHP-JWT |
| **安全防护** | 自定义 WAF + HTMLPurifier |

### 架构模式

```
┌─────────────────────────────────────────────────────────┐
│                      入口 index.php                      │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                    Kernel/Kernel.php                    │
│  ┌─────────┐  ┌──────────┐  ┌─────────┐  ┌──────────┐  │
│  │ 路由解析 │  │ 数据库初始化│  │  WAF防火墙 │  │ 插件系统  │  │
│  └─────────┘  └──────────┘  └─────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                     MVC 架构                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Controller  │  │    Model      │  │    View       │  │
│  │  (控制器层)    │  │   (数据模型)   │  │   (模板渲染)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Service    │  │  Interceptor  │  │  Annotation   │  │
│  │   (业务服务)  │  │   (拦截器)    │  │   (注解系统)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 核心目录结构

```
acg-faka/
├── app/                      # 应用程序目录
│   ├── Controller/           # 控制器层
│   │   ├── Admin/            # 后台管理控制器
│   │   ├── User/             # 前台用户控制器
│   │   ├── Shared/           # 共享控制器
│   │   └── Base/             # 基础控制器
│   ├── Model/                # 数据模型（Eloquent ORM）
│   ├── Service/              # 业务逻辑服务层
│   ├── View/                 # 视图模板（Smarty）
│   ├── Entity/               # 实体类
│   ├── Pay/                  # 支付相关
│   ├── Interceptor/          # 拦截器/中间件
│   ├── Util/                 # 工具类
│   └── Consts/               # 常量定义
├── kernel/                   # 核心框架目录
│   ├── Annotation/           # 注解收集器
│   ├── Component/            # 组件容器
│   ├── Container/            # 依赖注入容器
│   ├── Context/              # 请求上下文
│   ├── Database/             # 数据库相关
│   ├── Plugin/               # 插件系统
│   ├── Util/                 # 框架工具类
│   ├── Waf/                  # Web应用防火墙
│   └── Plugin.php            # 插件钩子管理
├── config/                   # 配置文件目录
│   ├── app.php               # 应用配置
│   ├── database.php          # 数据库配置
│   └── dependencies.php      # 依赖配置
└── vendor/                   # Composer依赖
```

### 主要依赖包

```json
{
  "require": {
    "php": ">=8.0.0",
    "smarty/smarty": "~3.1",
    "illuminate/database": "^7.30.6",
    "symfony/var-dumper": "^5.1",
    "illuminate/pagination": "^7.30.6",
    "rah/danpu": "^2.7",
    "phpmailer/phpmailer": "^6.5",
    "guzzlehttp/guzzle": "^7.4",
    "mrgoon/aliyun-sms": "^2.0",
    "firebase/php-jwt": "^6.11",
    "ezyang/htmlpurifier": "^4.18"
  }
}
```

---

## 功能简介

- **支付系统**：拥有强悍的插件扩展能力，支持全网任意平台、任意支付渠道
- **云更新**：系统升级新版本，无需繁琐操作，在店铺后台即可无缝完成升级
- **商品销售**：支持商品配图、会员价、游客价、邮件通知、卡密预选、API对接、强制登录购买、自定义控件功能、限时秒杀、批发优惠、优惠卷等众多功能
- **分站系统**：前台用户可以开通分站，分站可以独立运行，也可以卖主站商品
- **会员系统**：会员/商户融为一体，支持会员等级以及商户等级完全自定义
- **推广/代理系统**：拥有三级分销返佣功能，注册账号即实现自动发展下级
- **共享店铺系统**：可以在后台直接对接别人的店铺，通过扣除余额来进行无感知进货
- **应用商店**：拥有众多插件以及模板，让店铺变得格外强大
- **界面美观**：完美支持PC和手机，真正的内外二次元文化

---

## Ubuntu 22.04 + 宝塔面板 快速部署指南

### 第一步：环境准备

**1. 安装宝塔面板（如果尚未安装）**

```bash
# Ubuntu 22.04 安装宝塔面板
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh
sudo bash install.sh
```

**2. 安装所需软件**

在宝塔面板 → 首页 → 一键部署 中安装：

- **Nginx** (Web服务器)
- **MySQL 8.0** (数据库)
- **PHP 8.0+** (语言环境)

### 第二步：创建网站

1. 登录宝塔面板
2. 点击 **网站** → **添加站点**
3. 配置：
   - 域名：`yourdomain.com`
   - 根目录：`/www/wwwroot/acg-faka`
   - PHP版本：**PHP 8.0** 或更高
   - 数据库：创建 MySQL 数据库（如 `acg_faka`）

### 第三步：配置 PHP 扩展

在宝塔面板 → **软件商店** → **PHP 8.x** → **设置** → **安装扩展**：

**必需扩展：**

| 扩展 | 说明 |
|------|------|
| `openssl` | 加密支持 |
| `pdo` | 数据库抽象层 |
| `pdo_mysql` | MySQL驱动 |
| `json` | JSON处理 |
| `mbstring` | 多字节字符串 |
| `curl` | HTTP请求 |
| `gd` | 图片处理 |
| `zip` | 压缩支持 |

### 第四步：上传/部署源码

**方式一：直接上传**

1. 将项目源码打包上传到 `/www/wwwroot/acg-faka/`
2. 解压并确保文件权限正确：

```bash
cd /www/wwwroot/acg-faka
chown -R www:www ./
chmod -R 755 ./
```

**方式二：Composer 安装**

```bash
cd /www/wwwroot/acg-faka
composer install --no-dev
```

### 第五步：配置数据库

1. 导入数据库：在宝塔面板 → **数据库** → 选择数据库 → **导入**
2. 上传项目中的 `kernel/Install/Install.sql` 文件

3. 修改配置文件 `config/database.php`：

```php
<?php
return [
    'driver' => 'mysql',
    'host' => '127.0.0.1',
    'database' => 'acg_faka',      // 你的数据库名
    'username' => 'your_db_user',   // 数据库用户名
    'password' => 'your_db_pass',   // 数据库密码
    'charset' => 'utf8mb4',
    'collation' => 'utf8mb4_unicode_ci',
    'prefix' => 'acg_',             // 表前缀
];
```

### 第六步：配置 Nginx 伪静态

在宝塔面板 → **网站** → 找到你的站点 → **设置** → **伪静态**，选择 `thinkphp` 规则或手动添加：

```nginx
location / {
    if (!-e $request_filename){
        rewrite ^(.*)$ /index.php?s=$1 last;
        break;
    }
}
```

或者在站点的 **配置文件** 中手动修改（位于 `location / {}` 块内）：

```nginx
location / {
    try_files $uri $uri/ /index.php?s=$uri&$query_string;
}
```

### 第七步：安装和访问

1. 访问你的域名：`http://yourdomain.com`
2. 系统会自动跳转到安装页面
3. 按照引导完成安装
4. 安装完成后，后台地址：`http://yourdomain.com/admin`

---

## 伪静态规则配置

### Nginx 伪静态规则

```nginx
location / {
    if (!-e $request_filename){
        rewrite ^(.*)$ /index.php?s=$1 last;
        break;
    }
}
```

### IIS 伪静态规则

```xml
<rules>
    <rule name="acg_rewrite" stopProcessing="true">
        <match url="^(.*)$"/>
        <conditions logicalGrouping="MatchAll">
            <add input="{HTTP_HOST}" pattern="^(.*)$"/>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true"/>
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true"/>
        </conditions>
        <action type="Rewrite" url="index.php?s={R:1}"/>
    </rule>
</rules>
```

### Apache 伪静态

项目根目录已自带 `.htaccess` 文件，Apache 用户无需额外配置。

---

## 常见问题排查

| 问题 | 解决方案 |
|------|----------|
| 页面空白 | 检查 PHP 版本是否 >= 8.0，确认已安装所有必需扩展 |
| 数据库连接失败 | 检查 `config/database.php` 中的数据库凭据是否正确 |
| 伪静态不生效 | 确认 Nginx 伪静态规则已正确配置，重启 Nginx |
| 权限错误 | 执行 `chown -R www:www /www/wwwroot/acg-faka` |
| 验证码不显示 | 确认 GD 扩展已安装 |

---

## 快速体验

- **后台演示**：http://162.14.111.118:91/admin
  - 账号：demo@demo.com
  - 密码：123456

- **前台演示**：http://162.14.111.118:91
  - 账号：为了明天美好而战斗
  - 密码：123456

---

## 更多支持

- 交流QQ群：970103572
- Telegram：http://t.me/mcyofficial

---

## 法律声明

> 本商城程序基于 MIT 协议开源，并且完全免费。该程序的初衷是为开发者提供学习和研究的机会。未取得合法资质，严禁将本程序用于任何商业用途，尤其是禁止利用本程序搭建平台进行商品销售。
>
> 用户在使用或学习本程序时，必须严格遵守法律法规。我们提倡依法行事，尊重法律，坚守法律，避免对社会产生不良影响。
>
> 使用本程序即表示您已充分理解并同意本法律声明的所有内容。
